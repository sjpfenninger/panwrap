{
    // Path to pandoc binary
    "pandoc_path": "/usr/local/bin",

    // LaTeX binary path
    "tex_path": "/usr/texbin",

    // Preview tool, default is 'open -a Marked' to open Marked on OS X
    "preview": "open -a Marked",

    // PDF viewer, default of 'open' works for Mac OS X only
    "pdf_viewer": "open"
}